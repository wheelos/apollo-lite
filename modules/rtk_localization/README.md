# RTK Localization

This standalone component follows the repository runtime layout:

- `launch/` contains the module launcher.
- `dag/` contains the Cyber component graph.
- `conf/` contains runtime configuration.
- `proto/` contains the component-owned configuration schema.

Build the component with:

```bash
bash apollo.sh build rtk_localization
```
