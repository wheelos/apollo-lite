# User-maintained whl overrides for this clone.
# Keep only user-chosen, mode-scoped values here; auto-detected/runtime values belong in docker/.env.<mode>.local.

DEV_USE_GPU=true
DEV_SHM_SIZE=2g


TEST_SERVER_PORT=19001
TEST_CPUS=4
TEST_MEMORY=8G
TEST_USE_GPU=true
TEST_SHM_SIZE=2g
