# Environment Setup Context

Store environment bootstrap knowledge here: container entry, dependency setup, host requirements, and local environment caveats.

## Index

- [WHL startup and test-container workflow](whl-startup-and-test-container-workflow.md) - host setup, `whl` mode startup, and the rule to run validation from the managed `test` container.
