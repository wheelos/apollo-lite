# Copy this file to docker/.env.prod and adjust values for your production deployment.
# docker/scripts/whl.sh will auto-merge docker/.env.prod in prod mode,
# or fall back to this template when docker/.env.prod is absent.

APOLLO_RUNTIME_MODE=prod
GLOG_minloglevel=1
#APOLLO_CONF_PATH=/apollo/conf
